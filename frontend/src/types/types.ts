export type User = {
  name: string;
  email: string;
  password: string;
};
export type BillItem = {
  productName: string;
  productQty: number;
  productRate: number;
  total: number;
};

export type Bill = {

}